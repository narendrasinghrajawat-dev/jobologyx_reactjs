# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables — real credentials live under env/, split by target.
# Only the env/*.example templates are tracked; everything else here is not.
env/.env.test
env/.env.production
.env
.env.local

# Vercel CLI project link (only created if you ever run `vercel` locally)
.vercel

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
